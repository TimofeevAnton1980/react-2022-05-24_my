import { RootState } from "../../store";
export declare function loadProductIfNot(productId: string): (dispatch: import("redux").Dispatch<import("redux").AnyAction> | undefined, getState: () => RootState) => void;
